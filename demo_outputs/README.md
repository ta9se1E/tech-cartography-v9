# Cloud Run demo output bundle (Phase 24.6A)

Tracked minimal artifacts for `US-12565719-B2` demo mode on Cloud Run.

Regenerate from local `outputs/`:

```bash
python scripts/bundle_demo_outputs.py
```

Cloud Run env:

```bash
DEMO_OUTPUTS_ROOT=demo_outputs
```
